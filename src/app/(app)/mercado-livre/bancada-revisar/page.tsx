import Auditoria from "../auditoria/page";

/**
 * Bancada dos PEDIDOS A REVISAR — `/mercado-livre/bancada-revisar`.
 *
 * O `import "../bancadaAmostra"` saiu daqui pelo mesmo motivo da bancada dos
 * anúncios; ver `../bancada/page.tsx`.
 */
export default function BancadaDosPedidosARevisar() {
  return <Auditoria />;
}
