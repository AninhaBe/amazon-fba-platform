import { MercadoLivreWorkspace } from "../../../components/MercadoLivreWorkspace";

/**
 * Bancada do MONITOR DA CONTA — `/mercado-livre/bancada-monitor`.
 *
 * Monta a tela real, pelos motivos inteiros em `../bancada/page.tsx`.
 */
export default function BancadaDoMonitor() {
  return <MercadoLivreWorkspace view="monitor" />;
}
